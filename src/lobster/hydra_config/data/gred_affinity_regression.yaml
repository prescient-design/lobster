_target_: lobster.data._gred_affinity_regression_datamodule.GredAffinityStreamingDataModule

target_column: affinity_pkd # "affinity_pkd" or "expression_yield"
max_length: ${training.max_length}
batch_size: ${training.batch_size}
num_workers: ${training.num_workers}
pin_memory: true
seed: 42
significant_figures: 3
cache_dir: ${oc.env:LOBSTER_DATA_DIR}
val_split_key: test  # explicitly use 'test' for validation if 'val' is absent
