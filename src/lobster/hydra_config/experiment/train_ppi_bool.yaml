# @package _global_

defaults:
  - override /model: ppi.yaml

paths:
  root_dir: s3://prescient-data-dev/sandbox/jorent/plm-eval/ppi_boolean


data:
  # root: '../../../../../../../data/jorent/plm_eval/ppi'
  root: s3://prescient-pcluster-data/lobster/datasets/ppi
  batch_size: 4
  num_workers: 1
  lengths: [0.7,0.2,0.1]
  max_length: 512
  collate_fn:
    _target_: lobster.data._collate.ESMBatchConverterPPI
    contact_maps: false


model:
  checkpoint: ???

trainer:
  max_epochs: 100
  accelerator: "gpu"

callbacks:
  early_stopping:
    monitor: "val/loss"
    patience: 15
  model_checkpoint:
    monitor: "val/loss"

logger:
  project: lobster-eval
  entity: prescient-design
