defaults:
  - model_checkpoint.yaml
  - lr_monitor.yaml
  - backbone_reconstruction.yaml
  - flow_backbone_sampling.yaml
  - _self_

model_checkpoint:
  dirpath: ${paths.output_dir}
  filename: "{epoch}-{step}-{val_loss:.4f}"
  monitor: val_loss

early_stopping:
  monitor: val_loss
