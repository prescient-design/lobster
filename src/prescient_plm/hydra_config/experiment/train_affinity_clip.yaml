# @package _global_

defaults:
  - override /model: clip.yaml
  - override /data: affinity.yaml

paths:
  root_dir: s3://prescient-data-dev/sandbox/freyn6/prescient-plm/models/clip

data:
  root: /scratch/site/u/freyn6/data/affinity/

trainer:
  max_steps: 100_000
  val_check_interval: 0.25
  log_every_n_steps: 1

model:
  lr: 1e-3
